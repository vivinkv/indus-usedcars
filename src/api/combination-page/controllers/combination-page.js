'use strict';

/**
 * combination-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::combination-page.combination-page');
